#!/bin/sh
# simple iconv replacment for webmin
[ "$#" -ne "4" -o "$1" != "-f" -o "$3" != "-t" ] && echo "not run from webmin" 1>&2 && exit 1
[ ! -x "/opt/bin/recode" ] && echo "recode not found" 1>&2 && cat && exit 1
echo "[$2..$4]" 1>&2
/opt/bin/recode -f [$2..$4]
